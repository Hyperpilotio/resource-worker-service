apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: resource-worker-deployment
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: resource-worker
    spec:
      containers:
      - name: resource-worker
        image: gcr.io/hyperpilot-test/resource-worker-service
        ports:
        - containerPort: 7998
